#ifndef __watcher
#define __watcher

#include "types.h"

class watcher
{
private:
        ull executeTime;
        ull flushTime;
        ull totalRef;
        ull instRef
        ull dataRef
public:
        watcher();
};

#endif
