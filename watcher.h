#ifndef __watcher
#define __watcher

#include "types.h"

class watcher
{
public:
        ull executeTime;
        ull flushTime;
        ull totalRef;
        ull instRef
        ull dataRef

        watcher();
};

#endif
